---
layout: layout.njk
title: {{ title }}
permalink: /
---

{% set phone = (footer.phone or site.phone or "(949) 324-3565") %}
{% set phoneDigits = phone | replace('(','') | replace(')','') | replace(' ','') | replace('-', '') %}
{% set jobber = footer.jobber or site.jobber_url or "https://jobber.page.link/YourJobberLinkHere" %}

<!-- Hero -->
<section class="relative overflow-hidden bg-navy">
  <div class="absolute inset-0 hero-gradient"></div>
  <div class="relative max-w-7xl mx-auto px-4 py-16 md:py-24">
    <div class="grid md:grid-cols-2 gap-10 items-center">

      <div>
        <div class="flex flex-wrap gap-2 mb-5">
          {% for b in (hero.badges or ['Veteran-owned & operated','IICRC-certified technicians','Insurance claims support']) %}
          <span class="badge text-white text-xs font-semibold px-3 py-1 rounded-full">{{ b }}</span>
          {% endfor %}
        </div>

        <h1 class="text-white text-4xl md:text-5xl font-extrabold leading-tight">
          {{ hero.heading or '24/7 Water Damage Restoration — fast response, clean work, clear communication.' }}
        </h1>

        <p class="text-gray-200 text-lg mt-5 max-w-xl">
          {{ hero.body or 'From emergency water extraction to structural drying and rebuild-ready cleanup. We help you stop damage fast and get back to normal.' }}
        </p>

        <div class="mt-8 flex flex-col sm:flex-row gap-3">
          {% for btn in hero.buttons %}
          <a href="{{ btn.url }}" class="inline-flex items-center justify-center px-6 py-3 rounded-lg {{ 'bg-white text-navy font-extrabold ring-red' if loop.first else 'border border-white border-opacity-30 text-white font-extrabold hover:bg-white hover:bg-opacity-10' }}">{{ btn.text }}</a>
          {% endfor %}
        </div>

        <div class="mt-8 grid grid-cols-3 gap-4 text-white">
          {% for m in (hero.metrics or [
            {"value":"60 min","label":"Typical dispatch window"},
            {"value":"24/7","label":"Emergency availability"},
            {"value":"Local","label":"San Diego County team"}
          ]) %}
          <div class="glass rounded-xl p-4">
            <div class="text-2xl font-extrabold">{{ m.value }}</div>
            <div class="text-xs text-gray-200">{{ m.label }}</div>
          </div>
          {% endfor %}
        </div>
      </div>

      <!-- Right-side quick form card (mock) -->
      <div class="bg-white rounded-2xl p-6 md:p-8 shadow-soft">
        <div class="flex items-start justify-between gap-3">
          <div>
            <div class="text-sm font-semibold text-red">{{ hero.quickCard.kicker or 'Emergency? We can help.' }}</div>
            <h2 class="text-2xl font-extrabold mt-1">{{ hero.quickCard.heading or 'Request Service' }}</h2>
            <p class="text-gray-600 text-sm mt-2">{{ hero.quickCard.note or 'Use this as a simple inquiry form — or replace with a Jobber embed/button.' }}</p>
          </div>
          <div class="h-10 w-10 rounded-xl bg-gray-100 flex items-center justify-center">🛠️</div>
        </div>

        <form class="mt-6 grid grid-cols-1 gap-3" name="quick-request" method="POST" data-netlify="true" netlify-honeypot="bot-field">
          <input type="hidden" name="form-name" value="quick-request" />
          <p class="hidden">
            <label>Don’t fill this out: <input name="bot-field" /></label>
          </p>
          <input name="name" class="w-full px-4 py-3 rounded-lg border" placeholder="Name" />
          <input name="phone" class="w-full px-4 py-3 rounded-lg border" placeholder="Phone" />
          <select name="need" class="w-full px-4 py-3 rounded-lg border">
            <option>What do you need?</option>
            <option>Water damage restoration</option>
            <option>Sewage cleanup</option>
            <option>Structural drying</option>
            <option>Mold remediation</option>
            <option>Pack-out / contents</option>
            <option>Selective demolition</option>
            <option>Post-construction cleaning</option>
          </select>
          <textarea name="details" class="w-full px-4 py-3 rounded-lg border" rows="3" placeholder="Tell us what happened…"></textarea>
          <div class="mt-2 flex gap-3">
            {% for btn in hero.quickCard.buttons %}
            <a href="{{ btn.url }}" class="{{ 'flex-1 ' if loop.first }}inline-flex items-center justify-center px-5 py-3 rounded-lg {{ 'bg-navy2 text-white font-extrabold' if loop.first else 'border font-extrabold' }}">{{ btn.text }}</a>
            {% endfor %}
          </div>
          <div class="mt-4 text-xs text-gray-500">Submitting this form will email you via Netlify Forms (optional). You can remove it if you want Jobber-only.</div>
        </form>
      </div>

    </div>
  </div>
</section>

<!-- Services -->
<section id="services" class="bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 py-16">
    <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6">
      <div>
        <h2 class="text-3xl md:text-4xl font-extrabold">Core Services</h2>
        <p class="text-gray-600 mt-2 max-w-2xl">{{ services_intro or 'The restoration work we do every day — built around speed, safety, and documentation.' }}</p>
      </div>
      <a href="{{ services_button.url or '#contact' }}" class="inline-flex items-center justify-center px-4 py-2 rounded-lg border font-semibold hover:bg-white">{{ services_button.text or 'Check Availability' }}</a>
    </div>

    <div class="mt-10 grid md:grid-cols-3 gap-6">
      {% for s in (core_services_primary or [
        {"icon":"💧","title":"Water Damage Restoration","desc":"Water extraction, moisture mapping, containment, drying, and monitoring.","bullets":["Emergency extraction","Drying equipment placement","Daily moisture checks"]},
        {"icon":"🧪","title":"Sewage Cleanup","desc":"Category 3 cleanup with safety-first procedures and proper disposal.","bullets":["Containment & sanitation","Removal of affected materials","Deodorization"]},
        {"icon":"🌀","title":"Structural Drying","desc":"Dehumidification, airflow management, and verified dry standards.","bullets":["ASD-informed drying plans","Temperature & humidity control","Documentation for insurance"]}
      ]) %}
      <div class="bg-white rounded-2xl p-6 shadow-soft">
        <div class="h-10 w-10 rounded-xl bg-gray-100 flex items-center justify-center">{{ s.icon }}</div>
        <h3 class="font-extrabold text-xl mt-4">{{ s.title }}</h3>
        <p class="text-gray-600 mt-2">{{ s.desc }}</p>
        {% if s.bullets %}
        <ul class="mt-4 text-sm text-gray-700 list-disc pl-5 space-y-1">
          {% for li in s.bullets %}<li>{{ li }}</li>{% endfor %}
        </ul>
        {% endif %}
      </div>
      {% endfor %}
    </div>

    <div class="mt-6 grid md:grid-cols-2 gap-6">
      {% for s in (core_services_secondary or [
        {"icon":"🦠","title":"Mold Remediation","desc":"Containment, HEPA filtration, removal, and prevention guidance."},
        {"icon":"🧾","title":"Insurance Claims Assistance","desc":"Photos, notes, moisture readings, and scopes that help keep the claim moving."}
      ]) %}
      <div class="bg-white rounded-2xl p-6 shadow-soft">
        <div class="flex items-start gap-4">
          <div class="h-10 w-10 rounded-xl bg-gray-100 flex items-center justify-center">{{ s.icon }}</div>
          <div>
            <h3 class="font-extrabold text-xl">{{ s.title }}</h3>
            <p class="text-gray-600 mt-1">{{ s.desc }}</p>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- Additional Services -->
<section id="additional" class="bg-white">
  <div class="max-w-7xl mx-auto px-4 py-16">
    <div class="grid md:grid-cols-2 gap-8 items-center">
      <div>
        <h2 class="text-3xl md:text-4xl font-extrabold">Additional Services</h2>
        <p class="text-gray-600 mt-3">{{ additional_intro or 'To keep your project moving, we also handle pre/post work surrounding mitigation and rebuild.' }}</p>

        <div class="mt-6 space-y-3">
          {% for a in (additional_services or [
            {"icon":"📦","title":"Pack-outs (Contents)","desc":"Inventory, protection, off-site storage coordination, and return."},
            {"icon":"🧱","title":"Selective Demolition","desc":"Controlled removal of affected drywall, flooring, and cabinetry to access wet cavities."},
            {"icon":"🧼","title":"Post-construction Cleaning","desc":"Final clean for dust, debris, and surfaces so the property is move-in ready."}
          ]) %}
          <div class="flex items-start gap-3">
            <div class="h-9 w-9 rounded-xl bg-gray-100 flex items-center justify-center">{{ a.icon }}</div>
            <div>
              <div class="font-extrabold">{{ a.title }}</div>
              <div class="text-sm text-gray-600">{{ a.desc }}</div>
            </div>
          </div>
          {% endfor %}
        </div>

        <div class="mt-8 flex gap-3">
          {% for btn in additional_buttons %}
          <a href="{{ btn.url }}" class="inline-flex items-center justify-center px-5 py-3 rounded-lg {{ 'bg-navy2 text-white font-extrabold' if loop.first else 'border font-extrabold' }}">{{ btn.text }}</a>
          {% endfor %}
        </div>
      </div>

      <div class="bg-gray-50 rounded-2xl p-6 md:p-8 border shadow-soft">
        <h3 class="font-extrabold text-2xl">A clearer project flow</h3>
        <p class="text-gray-600 mt-2">{{ process_intro or 'We keep things simple: assess, document, mitigate, and leave the site rebuild-ready.' }}</p>

        <div class="mt-6 space-y-4">
          {% for step in (process_steps or [
            {"title":"Emergency call + dispatch","desc":"We confirm details and mobilize the right equipment."},
            {"title":"Assessment + moisture map","desc":"We measure, document, and explain options."},
            {"title":"Mitigation + drying","desc":"Extraction, containment, equipment placement, and monitoring."},
            {"title":"Demo + clean + ready for rebuild","desc":"Selective demo, pack-outs if needed, and post-construction clean."}
          ]) %}
          <div class="flex gap-3">
            <div class="h-9 w-9 rounded-full bg-navy2 text-white font-extrabold flex items-center justify-center">{{ loop.index }}</div>
            <div>
              <div class="font-extrabold">{{ step.title }}</div>
              <div class="text-sm text-gray-600">{{ step.desc }}</div>
            </div>
          </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Our Story -->
<section id="story" class="bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 py-16">
    <div class="grid md:grid-cols-2 gap-10 items-center">
      <div class="bg-white rounded-2xl p-8 shadow-soft">
        <div class="text-sm font-semibold text-red">{{ our_story.kicker or 'Our Story' }}</div>
        <h2 class="text-3xl font-extrabold mt-2">{{ our_story.heading or 'Why we built Flood QRF' }}</h2>
        <p class="text-gray-600 mt-4">{{ our_story.body or 'Restoration is stressful — the best companies reduce chaos. We’re a veteran-owned team that shows up quickly, communicates clearly, and documents everything so you can make decisions with confidence.' }}</p>
        <div class="mt-6 grid grid-cols-2 gap-4">
          <div class="rounded-xl bg-gray-50 p-4 border">
            <div class="font-extrabold">Mission</div>
            <div class="text-sm text-gray-600">{{ our_story.mission or 'Minimize loss and return safe living conditions fast.' }}</div>
          </div>
          <div class="rounded-xl bg-gray-50 p-4 border">
            <div class="font-extrabold">Values</div>
            <div class="text-sm text-gray-600">{{ our_story.values or 'Integrity, readiness, and meticulous work.' }}</div>
          </div>
        </div>
        <div class="mt-7">
          <a href="{{ our_story.button.url or '#contact' }}" class="inline-flex items-center justify-center px-5 py-3 rounded-lg bg-navy2 text-white font-extrabold">{{ our_story.button.text or 'Talk to the Team' }}</a>
        </div>
      </div>

      <div>
        <h3 class="text-2xl font-extrabold">What makes this site “deeper”</h3>
        <ul class="mt-4 space-y-3 text-gray-700">
          {% for item in (our_story.bullets or [
            "Story-led modules that increase trust and time on page.",
            "Team section with real faces and certifications.",
            "Blog previews for SEO (insurance tips, emergency checklists, etc.).",
            "Additional Services showcased clearly (pack-outs, demo, post-clean)."
          ]) %}
          <li class="flex gap-3"><span>✅</span><span><strong>{{ item }}</strong></span></li>
          {% endfor %}
        </ul>
        <div class="mt-6 p-5 rounded-2xl bg-white border shadow-soft">
          <div class="font-extrabold">Suggested Sitemap</div>
          <div class="text-sm text-gray-600 mt-1">Home • Services • Additional Services • Our Story • Team • Blog • Contact</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Team -->
<section id="team" class="bg-white">
  <div class="max-w-7xl mx-auto px-4 py-16">
    <div class="flex items-end justify-between gap-6">
      <div>
        <h2 class="text-3xl md:text-4xl font-extrabold">Meet the Team</h2>
        <p class="text-gray-600 mt-2">{{ team_intro or 'Use real names/photos later — here are mock cards showing the layout.' }}</p>
      </div>
      <a href="{{ team_button.url or '#contact' }}" class="hidden md:inline-flex items-center justify-center px-4 py-2 rounded-lg border font-semibold hover:bg-gray-50">{{ team_button.text or 'Work with us' }}</a>
    </div>

    <div class="mt-10 grid md:grid-cols-3 gap-6">
      {% for t in (team_members or [
        {"role":"Founder / Response Lead","meta":"WRT • ASD • OSHA","quote":"We treat every home like it’s our own — fast, clean, and documented."},
        {"role":"Operations Manager","meta":"Scheduling • Claims • Customer care","quote":"Coordinates dispatch, daily updates, and insurance documentation."},
        {"role":"Lead Technician","meta":"Containment • Drying • Demo","quote":"Specializes in complex dry-outs and controlled selective demolition."}
      ]) %}
      <div class="rounded-2xl border bg-white p-6 shadow-soft">
        <div class="flex items-center gap-4">
          {% if t.image %}
          <img src="{{ t.image }}" alt="{{ t.name or t.role }}" class="h-14 w-14 rounded-2xl object-cover" />
          {% else %}
          <div class="h-14 w-14 rounded-2xl bg-gray-100 flex items-center justify-center text-xl font-bold text-gray-400">{{ (t.name | first) if t.name else '👤' }}</div>
          {% endif %}
          <div>
            {% if t.name %}<div class="font-extrabold">{{ t.name }}</div>{% endif %}
            <div class="{% if t.name %}text-sm text-gray-600{% else %}font-extrabold{% endif %}">{{ t.role }}</div>
            <div class="text-sm text-gray-600">{{ t.meta }}</div>
          </div>
        </div>
        <p class="text-gray-600 mt-4 text-sm">“{{ t.quote }}”</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- Blog -->
<section id="blog" class="bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 py-16">
    <div class="flex items-end justify-between gap-6">
      <div>
        <h2 class="text-3xl md:text-4xl font-extrabold">From the Blog</h2>
        <p class="text-gray-600 mt-2">{{ blog_intro or 'A lightweight blog gives you consistent SEO growth and content to share with customers.' }}</p>
      </div>
      <a href="/blog/" class="inline-flex items-center justify-center px-4 py-2 rounded-lg border font-semibold hover:bg-white">View all posts</a>
    </div>

    <div class="mt-10 grid md:grid-cols-3 gap-6">
      {% for p in (blog_posts or [
        {"meta":"Checklist • 6 min read","title":"What to do in the first 60 minutes after a leak","desc":"A calm, step-by-step plan that helps reduce damage and protects your claim.","href":"/blog/first-60-minutes/"},
        {"meta":"Insurance • 8 min read","title":"How to document water damage for insurance","desc":"Photos, moisture readings, and what adjusters typically need to see.","href":"/blog/insurance-documentation/"},
        {"meta":"Safety • 5 min read","title":"Sewage cleanup: what’s safe to keep vs. remove","desc":"When porous materials must go — and how we handle disposal and sanitation.","href":"/blog/sewage-safety/"}
      ]) %}
      <article class="rounded-2xl bg-white border p-6 shadow-soft">
        <div class="text-xs text-gray-500">{{ p.meta }}</div>
        <h3 class="font-extrabold text-xl mt-2">{{ p.title }}</h3>
        <p class="text-gray-600 mt-2 text-sm">{{ p.desc }}</p>
        <a class="mt-4 inline-flex font-extrabold text-red" href="{{ p.href }}">Read →</a>
      </article>
      {% endfor %}
    </div>
  </div>
</section>

<!-- Contact / Service Area -->
<section id="contact" class="bg-white">
  <div class="max-w-7xl mx-auto px-4 py-16">
    <div class="grid md:grid-cols-2 gap-10">
      <div>
        <h2 class="text-3xl md:text-4xl font-extrabold">Service Area + Contact</h2>
        <p class="text-gray-600 mt-2">We serve all of San Diego County. For emergencies, calling is the fastest.</p>
        <div class="mt-6 space-y-3">
          <div class="rounded-2xl border p-5">
            <div class="text-xs text-gray-500">24/7 Emergency Line</div>
            <a class="text-2xl font-extrabold text-navy" href="tel:{{ phoneDigits }}">{{ phone }}</a>
            <div class="text-sm text-gray-600 mt-1">Or tap “Request Service” to open Jobber scheduling.</div>
            <div class="mt-4 flex gap-3">
              {% for btn in contact_buttons %}
              <a class="inline-flex items-center justify-center px-5 py-3 rounded-lg {{ 'bg-navy2 text-white font-extrabold' if loop.first else 'border font-extrabold' }}" href="{{ btn.url }}">{{ btn.text }}</a>
              {% endfor %}
            </div>
          </div>
          <div class="rounded-2xl border p-5 bg-gray-50">
            <div class="font-extrabold">Business Hours</div>
            <div class="text-sm text-gray-600 mt-1">{{ contact.hours or 'Emergency response 24/7 • Office: Mon–Fri 8am–5pm' }}</div>
          </div>
        </div>
      </div>

      <div class="rounded-2xl overflow-hidden border shadow-soft">
        <iframe
          title="Service Area Map"
          src="{{ map or site.map_embed }}"
          class="w-full h-96"
          loading="lazy"
          allowfullscreen
        ></iframe>
        <div class="p-4 bg-white text-sm text-gray-600">Map embed can be updated to your exact service area or office address.</div>
      </div>

    </div>
  </div>
</section>
