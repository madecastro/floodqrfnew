---
layout: layout.njk
title: {{ title }}
permalink: /
---

<section class="relative w-full overflow-hidden">
  <img src="/images/hero.png" alt="Flood QRF Hero" class="w-full h-[700px] md:h-[800px] lg:h-[900px] object-cover object-center" />
  <div class="absolute inset-0 bg-black bg-opacity-50 flex flex-col justify-center items-center text-center text-white px-4">
    <h1 class="text-5xl md:text-6xl font-bold mb-6">{{ hero.heading }}</h1>
    <p class="text-xl md:text-2xl text-gray-200 mb-8">{{ hero.subheading }}</p>
    <a href="tel:9493243565" class="bg-white text-blue-900 font-bold px-6 py-3 rounded shadow-lg hover:bg-gray-200 transition">
      📞 Call Now
    </a>
  </div>
</section>

<section class="py-20 bg-gray-50">
  <div class="max-w-6xl mx-auto text-center">
    <h2 class="text-4xl font-semibold mb-12">Our Services</h2>
    <div class="grid md:grid-cols-3 gap-12">
      {% for service in services %}
        <div class="flex flex-col items-center">
          {% if service.icon %}
            <img src="{{ service.icon }}" alt="{{ service.title }} Icon" class="h-32 mb-5" />
          {% endif %}
          <h3 class="font-bold text-2xl">{{ service.title }}</h3>
          <p class="text-lg text-gray-600 mt-3">{{ service.description }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="py-20 bg-white">
  <div class="max-w-6xl mx-auto px-4 text-center">
    <h2 class="text-4xl font-semibold mb-12">Why Choose Flood QRF</h2>
    <div class="grid md:grid-cols-3 gap-10 text-left">
      <div class="flex items-start space-x-4">
        <img src="/images/icon-shield.png" alt="Veteran Owned Icon" class="h-12 w-12" />
        <div>
          <h3 class="text-xl font-bold mb-1">Veteran-Owned & Operated</h3>
          <p class="text-gray-600">We bring discipline, reliability, and a commitment to service.</p>
        </div>
      </div>
      <div class="flex items-start space-x-4">
        <img src="/images/icon-clock.png" alt="24/7 Service Icon" class="h-12 w-12" />
        <div>
          <h3 class="text-xl font-bold mb-1">Rapid Emergency Response</h3>
          <p class="text-gray-600">Available 24/7 to contain damage fast and reduce loss.</p>
        </div>
      </div>
      <div class="flex items-start space-x-4">
        <img src="/images/icon-clipboard.png" alt="Insurance Help Icon" class="h-12 w-12" />
        <div>
          <h3 class="text-xl font-bold mb-1">Insurance Claims Support</h3>
          <p class="text-gray-600">We work directly with your insurance to simplify the process.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="py-20">
  <div class="max-w-6xl mx-auto px-4 grid md:grid-cols-2 gap-10 items-center">
    <div>
      <h3 class="text-3xl font-semibold mb-4">Service Area</h3>
      <p class="text-lg text-gray-700 mb-6">
        We serve all of San Diego County — fast response and reliable support.
      </p>
    </div>
    <iframe src="{{ map }}" class="rounded-lg w-full h-80 border" allowfullscreen="" loading="lazy"></iframe>
  </div>
</section>
