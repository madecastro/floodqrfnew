# Flood QRF — Option A Site (Eleventy)

## Local dev
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

## CMS (optional)
Decap CMS is included at `/admin/` (requires Netlify Identity + Git Gateway).
