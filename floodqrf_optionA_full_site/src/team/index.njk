---
layout: page.njk
title: Team
permalink: /team/
---

<div class="grid md:grid-cols-3 gap-6">
  {% for m in team_preview.members %}
    <div class="border rounded-lg p-6 bg-white shadow-sm">
      <div class="w-12 h-12 rounded-full bg-blue-900 text-white flex items-center justify-center font-bold text-lg">{{ m.name | first }}</div>
      <div class="mt-4 font-bold text-xl">{{ m.name }}</div>
      <div class="text-sm text-gray-600">{{ m.role }}</div>
      <p class="mt-3 text-gray-700">{{ m.bio }}</p>
    </div>
  {% endfor %}
</div>
