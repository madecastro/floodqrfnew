---
layout: layout.njk
permalink: /
---

{# ---------------------------
  Option A homepage (locked-in)
  Renders: hero + trust bar + services + additional services + how it works + story + team + blog + faq
  Notes:
  - Tailwind CDN (v2.2) is used, so we add a tiny bit of custom CSS for anchor offsets + gallery animation.
---------------------------- #}

{% set _footer = footer or {} %}
{% set phone_display = (_footer.phone or "(949) 324-3565") %}
{% set phone_tel = phone_display | replace("(", "") | replace(")", "") | replace(" ", "") | replace("-", "") %}
{% set jobber_link = (hero and hero.cta and hero.cta.secondary and hero.cta.secondary.href) or (_footer.jobber) or "https://jobber.page.link/YourJobberLinkHere" %}
{% set call_link = (hero and hero.cta and hero.cta.primary and hero.cta.primary.href) or ("tel:" + phone_tel) %}

<style>
  /* Anchor offsets for fixed header */
  .anchor-offset { scroll-margin-top: 96px; }

  /* FAQ toggle icon */
  .faq-plus { transition: transform .18s ease; }
  details[open] .faq-plus { transform: rotate(45deg); }

  /* Simple marquee-style gallery animation */
  @keyframes scrollGallery {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
  .animate-scroll-gallery { animation: scrollGallery 40s linear infinite; }
  @media (prefers-reduced-motion: reduce) {
    .animate-scroll-gallery { animation: none; }
  }
</style>

<!-- Hero Section -->
<section class="relative w-full overflow-hidden mt-20">
  <img
    src="{{ hero.image or '/images/hero.png' }}"
    alt="Flood QRF Hero"
    class="w-full max-h-screen object-cover object-center"
  />
  <div class="absolute inset-0 bg-black bg-opacity-50 flex items-center">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 w-full">
      <div class="grid lg:grid-cols-12 gap-8 items-center">
        <div class="lg:col-span-7 text-white">
          <h1 class="text-4xl md:text-6xl font-extrabold leading-tight">{{ hero.heading }}</h1>
          <p class="text-lg md:text-2xl text-gray-200 mt-5 max-w-2xl">{{ hero.subheading }}</p>

          {% if hero.badges and hero.badges.length %}
          <div class="mt-6 flex flex-wrap gap-2">
            {% for badge in hero.badges %}
              <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold bg-white bg-opacity-20 border border-white border-opacity-20">
                {{ badge }}
              </span>
            {% endfor %}
          </div>
          {% endif %}

          <div class="mt-8 flex flex-wrap gap-3">
            <a href="{{ call_link }}" class="inline-flex items-center bg-white text-blue-900 font-bold px-6 py-3 rounded shadow-lg hover:bg-gray-200 transition">
              📞 {{ hero.cta.primary.label or 'Call Now' }}
            </a>
            <a href="{{ jobber_link }}" class="inline-flex items-center bg-blue-700 text-white font-bold px-6 py-3 rounded shadow-lg hover:bg-blue-600 transition">
              {{ hero.cta.secondary.label or 'Request Service' }}
            </a>
          </div>
        </div>

        <!-- Quick action card (desktop) -->
        <div class="hidden lg:block lg:col-span-5">
          <div class="bg-white rounded-2xl shadow-xl p-6">
            <p class="text-sm font-semibold text-blue-900">Emergency? We’re on call 24/7.</p>
            <h2 class="text-2xl font-extrabold mt-2 text-gray-900">Get help now</h2>
            <p class="text-gray-600 mt-2">Call for immediate dispatch or request service to schedule a visit.</p>
            <div class="mt-5 grid gap-3">
              <a href="{{ call_link }}" class="w-full inline-flex justify-center items-center bg-blue-900 text-white font-bold px-5 py-3 rounded-lg hover:bg-blue-800">
                📞 Call {{ phone_display }}
              </a>
              <a href="{{ jobber_link }}" class="w-full inline-flex justify-center items-center bg-gray-100 text-blue-900 font-bold px-5 py-3 rounded-lg hover:bg-gray-200">
                Request Service
              </a>
            </div>
            <p class="mt-4 text-xs text-gray-500">We document drying with photos + moisture logs to support insurance claims.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Trust Bar -->
{% if trust_bar and trust_bar.length %}
<section class="bg-white border-b">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 py-8">
    <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
      {% for item in trust_bar %}
        <div class="rounded-xl bg-gray-50 border p-5">
          <p class="font-bold text-blue-900">{{ item.title }}</p>
          <p class="text-sm text-gray-600 mt-1">{{ item.description }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}

<!-- Services Section -->
<section id="services" class="anchor-offset py-20 bg-gray-50">
  <div class="max-w-6xl mx-auto text-center">
    <h2 class="text-4xl font-semibold mb-12">Our Services</h2>
    <div class="grid md:grid-cols-3 gap-12">
      {% for service in services %}
        <div class="flex flex-col items-center">
          {% if service.title == 'Water Extraction' %}
            <svg xmlns="http://www.w3.org/2000/svg" class="h-32 w-32 text-blue-800 mb-5" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M10 2C10 2 4 7 4 11a6 6 0 0012 0c0-4-6-9-6-9zM8 14a2 2 0 114 0 2 2 0 01-4 0z" clip-rule="evenodd" />
            </svg>
          {% elif service.title == 'Structural Drying' %}
            <svg xmlns="http://www.w3.org/2000/svg" class="h-32 w-32 text-blue-800 mb-5" viewBox="0 0 24 24" fill="currentColor">
              <path fill-rule="evenodd" d="M12 2a1 1 0 01.993.883L13 3v1.07a7.001 7.001 0 014.929 11.818l.708.708a1 1 0 01-1.32 1.497l-.094-.083-.707-.708A7.001 7.001 0 0113 19.93V21a1 1 0 01-1.993.117L11 21v-1.07a7.001 7.001 0 01-4.929-11.818l-.708-.708a1 1 0 011.32-1.497l.094.083.707.708A7.001 7.001 0 0111 4.07V3a1 1 0 011-1zM12 6a5 5 0 100 10 5 5 0 000-10z" clip-rule="evenodd" />
            </svg>
          {% elif service.title == 'Mold Remediation' %}
            <svg xmlns="http://www.w3.org/2000/svg" class="h-32 w-32 text-blue-800 mb-5" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2a1 1 0 01.993.883L13 3v1h1a1 1 0 01.117 1.993L14 6h-1v1a1 1 0 01-1.993.117L11 7V6H10a1 1 0 01-.117-1.993L10 4h1V3a1 1 0 011-1zM4 13a1 1 0 011-1h1v-1a1 1 0 011.993-.117L8 11v1h1a1 1 0 01.117 1.993L9 14H8v1a1 1 0 01-1.993.117L6 15v-1H5a1 1 0 01-1-1zm10 2a1 1 0 011-1h5a1 1 0 01.117 1.993L20 16h-5a1 1 0 01-1-1z" />
            </svg>
          {% else %}
            <svg xmlns="http://www.w3.org/2000/svg" class="h-32 w-32 text-blue-800 mb-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-6h6v6m2 4H7a2 2 0 01-2-2V5a2 2 0 012-2h10a2 2 0 012 2v14a2 2 0 01-2 2z" />
            </svg>
          {% endif %}
          <h3 class="font-bold text-2xl">{{ service.title }}</h3>
          <p class="text-lg text-gray-600 mt-3">{{ service.description }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- Additional Services -->
{% if additional_services and additional_services.items and additional_services.items.length %}
<section id="additional-services" class="anchor-offset py-20 bg-white">
  <div class="max-w-6xl mx-auto px-4 text-center">
    <h2 class="text-4xl font-semibold">{{ additional_services.heading or 'Additional Services' }}</h2>
    {% if additional_services.subheading %}
      <p class="text-lg text-gray-600 mt-4 max-w-3xl mx-auto">{{ additional_services.subheading }}</p>
    {% endif %}

    <div class="grid md:grid-cols-3 gap-8 mt-12">
      {% for item in additional_services.items %}
        <div class="rounded-2xl border bg-gray-50 p-8 text-left shadow-sm">
          <div class="flex items-start gap-4">
            <div class="w-10 h-10 rounded-xl bg-blue-900 text-white flex items-center justify-center font-bold">+</div>
            <div>
              <h3 class="text-2xl font-bold text-gray-900">{{ item.title }}</h3>
              <p class="text-gray-600 mt-2">{{ item.description }}</p>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>

    <div class="mt-10">
      <a href="{{ jobber_link }}" class="inline-flex bg-blue-900 text-white font-bold px-6 py-3 rounded shadow-lg hover:bg-blue-800 transition">Request Service</a>
    </div>
  </div>
</section>
{% endif %}

<!-- How it works -->
{% if how_it_works and how_it_works.steps and how_it_works.steps.length %}
<section class="py-20 bg-gray-50">
  <div class="max-w-6xl mx-auto px-4">
    <div class="text-center">
      <h2 class="text-4xl font-semibold">{{ how_it_works.heading or 'How it works' }}</h2>
    </div>
    <div class="grid md:grid-cols-4 gap-6 mt-12">
      {% for step in how_it_works.steps %}
        <div class="bg-white border rounded-2xl p-6 shadow-sm">
          <div class="text-blue-900 font-extrabold text-2xl">{{ loop.index }}</div>
          <h3 class="font-bold text-xl mt-2">{{ step.title }}</h3>
          <p class="text-gray-600 mt-2">{{ step.description }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}

<!-- Gallery Section -->
<section class="py-20 bg-gray-50">
  <div class="max-w-6xl mx-auto px-4">
    <h2 class="text-4xl font-semibold text-center mb-12">Gallery</h2>
    <div class="relative overflow-hidden">
      {# Duplicate the images so the marquee loop looks continuous #}
      <div class="flex animate-scroll-gallery space-x-4 w-max">
        <img src="/images/gallery-tech-customer.png" alt="Technician consulting with customer" class="h-64 w-auto object-cover rounded-lg shadow" />
        <img src="/images/gallery-truck-house.png" alt="Service truck outside home" class="h-64 w-auto object-cover rounded-lg shadow" />
        <img src="/images/gallery-water-extraction.png" alt="Residential water extraction" class="h-64 w-auto object-cover rounded-lg shadow" />
        <img src="/images/gallery-structural-drying.png" alt="Plastic-sealed drying area" class="h-64 w-auto object-cover rounded-lg shadow" />
        <img src="/images/gallery-demo.png" alt="Technicians removing drywall and flooring" class="h-64 w-auto object-cover rounded-lg shadow" />

        <img src="/images/gallery-tech-customer.png" alt="Technician consulting with customer" class="h-64 w-auto object-cover rounded-lg shadow" />
        <img src="/images/gallery-truck-house.png" alt="Service truck outside home" class="h-64 w-auto object-cover rounded-lg shadow" />
        <img src="/images/gallery-water-extraction.png" alt="Residential water extraction" class="h-64 w-auto object-cover rounded-lg shadow" />
        <img src="/images/gallery-structural-drying.png" alt="Plastic-sealed drying area" class="h-64 w-auto object-cover rounded-lg shadow" />
        <img src="/images/gallery-demo.png" alt="Technicians removing drywall and flooring" class="h-64 w-auto object-cover rounded-lg shadow" />
      </div>
    </div>
  </div>
</section>

<!-- IICRC Certification Section -->
<section class="py-20 bg-gray-100">
  <div class="max-w-6xl mx-auto px-4 text-center">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-20 w-20 mx-auto text-blue-800 mb-6" viewBox="0 0 24 24" fill="currentColor">
      <path fill-rule="evenodd" d="M12 2a1 1 0 01.894.553l2.382 4.825 5.324.774a1 1 0 01.554 1.706l-3.85 3.754.91 5.305a1 1 0 01-1.45 1.054L12 17.347l-4.764 2.524a1 1 0 01-1.45-1.054l.91-5.305-3.85-3.754a1 1 0 01.554-1.706l5.324-.774L11.106 2.553A1 1 0 0112 2z" clip-rule="evenodd" />
    </svg>
    <h2 class="text-4xl font-semibold mb-8">Certified Restoration Experts</h2>
    <p class="text-lg text-gray-700 max-w-3xl mx-auto">
      Every Flood QRF technician is certified by the <strong>IICRC</strong> as a Water Restoration Technician (WRT) and Applied Structural Drying (ASD) specialist. These industry-standard certifications ensure that our team has the technical knowledge, hands-on training, and experience needed to safely and effectively mitigate water damage — protecting your home and preventing long-term issues like mold and structural weakening.
    </p>
    <div class="mt-8">
      <a href="{{ call_link }}" class="inline-block bg-blue-800 text-white font-bold px-6 py-3 rounded shadow-lg hover:bg-blue-700 transition">📞 Call Now</a>
    </div>
  </div>
</section>

<!-- Testimonials Section -->
<section class="py-20 bg-white">
  <div class="max-w-6xl mx-auto px-4 text-center">
    <h2 class="text-4xl font-semibold mb-12">What Our Customers Say</h2>
    <div class="grid md:grid-cols-3 gap-8">
      <div class="bg-gray-100 p-6 rounded-lg shadow">
        <p class="text-lg text-gray-700 mb-4">“Flood QRF responded within an hour and prevented major damage to our home. Highly professional and courteous!”</p>
        <p class="font-bold text-blue-900">— Sarah M., San Diego</p>
      </div>
      <div class="bg-gray-100 p-6 rounded-lg shadow">
        <p class="text-lg text-gray-700 mb-4">“The team explained every step, worked directly with our insurance, and left everything dry and spotless.”</p>
        <p class="font-bold text-blue-900">— James K., Chula Vista</p>
      </div>
      <div class="bg-gray-100 p-6 rounded-lg shadow">
        <p class="text-lg text-gray-700 mb-4">“I was impressed by their speed and professionalism. Their certified techs gave us peace of mind.”</p>
        <p class="font-bold text-blue-900">— Lisa R., Escondido</p>
      </div>
    </div>
  </div>
</section>

<!-- Why Choose Flood QRF Section -->
<section class="py-20 bg-white">
  <div class="max-w-6xl mx-auto px-4 text-center">
    <h2 class="text-4xl font-semibold mb-12">Why Choose Flood QRF</h2>
    <div class="grid md:grid-cols-3 gap-12 text-center">
      <div class="flex flex-col items-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-32 w-32 text-blue-800 mb-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 11c.666-1.333 2-2 4-2s3.334.667 4 2c.666 1.333.666 2.667 0 4-.666 1.333-2 2-4 2s-3.334-.667-4-2m0 0c-.667-1.333-2-2-4-2s-3.333.667-4 2c-.667 1.333-.667 2.667 0 4 .667 1.333 2 2 4 2s3.333-.667 4-2m0-4V5" />
        </svg>
        <h3 class="font-bold text-2xl mb-2">Veteran-Owned & Operated</h3>
        <p class="text-lg text-gray-600">We bring discipline, reliability, and a commitment to service.</p>
      </div>

      <div class="flex flex-col items-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-32 w-32 text-blue-800 mb-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <h3 class="font-bold text-2xl mb-2">Rapid Emergency Response</h3>
        <p class="text-lg text-gray-600">Available 24/7 to contain damage fast and reduce loss.</p>
      </div>

      <div class="flex flex-col items-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-32 w-32 text-blue-800 mb-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-6h6v6m2 4H7a2 2 0 01-2-2V5a2 2 0 012-2h10a2 2 0 012 2v14a2 2 0 01-2 2z" />
        </svg>
        <h3 class="font-bold text-2xl mb-2">Insurance Claims Support</h3>
        <p class="text-lg text-gray-600">We work directly with your insurance to simplify the process.</p>
      </div>

    </div>
  </div>
</section>

<!-- Our Story Preview -->
{% if our_story and (our_story.heading or our_story.excerpt) %}
<section class="py-20 bg-gray-50">
  <div class="max-w-6xl mx-auto px-4 grid md:grid-cols-2 gap-10 items-center">
    <div>
      <h2 class="text-4xl font-semibold">{{ our_story.heading or 'Our Story' }}</h2>
      <p class="text-lg text-gray-700 mt-4">{{ our_story.excerpt }}</p>
      {% if our_story.cta and our_story.cta.href %}
        <div class="mt-6">
          <a href="{{ our_story.cta.href }}" class="inline-flex bg-blue-900 text-white font-bold px-6 py-3 rounded shadow hover:bg-blue-800">{{ our_story.cta.label or 'Read Our Story' }}</a>
        </div>
      {% endif %}
    </div>

    <div class="bg-white border rounded-2xl p-8 shadow-sm">
      <h3 class="text-xl font-bold text-blue-900">What you can expect</h3>
      <ul class="mt-4 space-y-3 text-gray-700">
        <li class="flex gap-3"><span class="text-blue-900 font-bold">✓</span><span>Clear communication and daily updates</span></li>
        <li class="flex gap-3"><span class="text-blue-900 font-bold">✓</span><span>Professional equipment + moisture monitoring</span></li>
        <li class="flex gap-3"><span class="text-blue-900 font-bold">✓</span><span>Documentation to support insurance claims</span></li>
        <li class="flex gap-3"><span class="text-blue-900 font-bold">✓</span><span>Respect for your home and belongings</span></li>
      </ul>
    </div>
  </div>
</section>
{% endif %}

<!-- Team Preview -->
{% if team_preview and team_preview.members and team_preview.members.length %}
<section class="py-20 bg-white">
  <div class="max-w-6xl mx-auto px-4 text-center">
    <h2 class="text-4xl font-semibold">{{ team_preview.heading or 'Meet the Team' }}</h2>
    <div class="grid md:grid-cols-3 gap-8 mt-12">
      {% for m in team_preview.members %}
        <div class="border rounded-2xl p-8 shadow-sm text-left">
          <div class="flex items-center gap-4">
            <img src="{{ m.photo or '/images/team-placeholder-1.png' }}" alt="{{ m.name }}" class="w-14 h-14 rounded-full object-cover border" />
            <div>
              <div class="font-bold text-xl">{{ m.name }}</div>
              <div class="text-sm text-blue-900 font-semibold">{{ m.role }}</div>
            </div>
          </div>
          <p class="text-gray-600 mt-4">{{ m.bio }}</p>
        </div>
      {% endfor %}
    </div>
    {% if team_preview.cta and team_preview.cta.href %}
      <div class="mt-10">
        <a href="{{ team_preview.cta.href }}" class="inline-flex bg-blue-900 text-white font-bold px-6 py-3 rounded shadow hover:bg-blue-800">{{ team_preview.cta.label or 'See the full team' }}</a>
      </div>
    {% endif %}
  </div>
</section>
{% endif %}

<!-- Blog Preview -->
{% if blog_preview and blog_preview.posts and blog_preview.posts.length %}
<section class="py-20 bg-gray-50">
  <div class="max-w-6xl mx-auto px-4">
    <div class="flex items-end justify-between gap-6 flex-wrap">
      <div>
        <h2 class="text-4xl font-semibold">{{ blog_preview.heading or 'From the Blog' }}</h2>
        <p class="text-gray-600 mt-2">Practical guidance for water losses, drying timelines, and insurance documentation.</p>
      </div>
      {% if blog_preview.cta and blog_preview.cta.href %}
        <a href="{{ blog_preview.cta.href }}" class="inline-flex bg-blue-900 text-white font-bold px-5 py-3 rounded shadow hover:bg-blue-800">{{ blog_preview.cta.label or 'Visit the Blog' }}</a>
      {% endif %}
    </div>

    <div class="grid md:grid-cols-3 gap-8 mt-10">
      {% for post in blog_preview.posts %}
        <a href="{{ post.href }}" class="block border rounded-2xl bg-white p-7 shadow-sm hover:shadow-md transition">
          <h3 class="font-bold text-xl text-gray-900">{{ post.title }}</h3>
          <p class="text-gray-600 mt-3">{{ post.excerpt }}</p>
          <div class="mt-5 text-blue-900 font-bold">Read more →</div>
        </a>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}

<!-- FAQ -->
{% if faq and faq.items and faq.items.length %}
<section class="py-20 bg-white">
  <div class="max-w-4xl mx-auto px-4">
    <h2 class="text-4xl font-semibold text-center">{{ faq.heading or 'FAQ' }}</h2>
    <div class="mt-10 space-y-4">
      {% for item in faq.items %}
        <details class="group border rounded-2xl p-6 bg-gray-50">
          <summary class="cursor-pointer list-none flex items-center justify-between font-bold text-lg">
            <span>{{ item.q }}</span>
            <span class="ml-4 text-blue-900 faq-plus">+</span>
          </summary>
          <p class="text-gray-700 mt-4">{{ item.a }}</p>
        </details>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}

<!-- Service Area Section -->
<section class="py-20">
  <div class="max-w-6xl mx-auto px-4 grid md:grid-cols-2 gap-10 items-center">
    <div>
      <h3 class="text-3xl font-semibold mb-4">Service Area</h3>
      <p class="text-lg text-gray-700 mb-6">
        We serve all of San Diego County — fast response and reliable support.
      </p>
      <div class="flex flex-wrap gap-3">
        <a href="{{ call_link }}" class="inline-flex bg-blue-900 text-white font-bold px-6 py-3 rounded shadow hover:bg-blue-800">📞 Call Now</a>
        <a href="{{ jobber_link }}" class="inline-flex bg-gray-100 text-blue-900 font-bold px-6 py-3 rounded shadow hover:bg-gray-200">Request Service</a>
      </div>
    </div>
    <iframe src="{{ map }}" class="rounded-lg w-full h-80 border" allowfullscreen="" loading="lazy"></iframe>
  </div>
</section>
